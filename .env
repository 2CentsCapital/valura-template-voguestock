# Public origin of the deployed page. index.html uses it for the canonical link and the
# Open Graph and Twitter image URLs. Override at build time if the page is served elsewhere:
#   VITE_SITE_URL=https://example.com npm run build
VITE_SITE_URL=https://voguestock.valura.ai
